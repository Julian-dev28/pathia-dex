# Optional. Everything works without this file — the app falls back to the
# public Base endpoints listed in src/lib/chain.ts. Set one only if you are
# running enough traffic to get rate-limited.
RPC_URL=https://mainnet.base.org
